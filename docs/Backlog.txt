FUNCIONALIDADES:

TAB cuando estamos en el grid con un combo abierto debe omitir la seleccion actual y pasar al siguiente elemento del grid.

Generar botones corporativos con una guia de estilo unificada.

La barra inferior de estado está mostrando logs. Debe ser más del estilo 'Procesando...' y 'Listo' (con iconos). El resto de los logs deben ir a un archivo de log.

Leer metadatos de Traktor (Analizado Si/No - Número de Cues) (Revisar funcionalidad)

Rehacer el Header: El logo debe contrastar más con el fondo y el texto debe ser más legible, seleccionar carpeta y actualizar deben desaparecer. Incorporar buscador de texto, mantener ruta actual. Incluir iconos (no botones) para actualizar, para filtrar por traktor o por cues (hacer los toggle modados corporativo), añadir icono para gestionar catálogos.

Integrar Spotify si no se encuentra en Discogs (para producciones nuevas)

Defecto de rendimiento (más de 60 peticiones a Discogs por minuto falla)